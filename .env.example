# Vibe Kanban Environment Configuration
# Copy this file to .env and adjust values as needed

# ============================================================================
# Git Repository Scanning Configuration
# ============================================================================

# Timeout for git repository scanning (milliseconds)
# This is the soft timeout - scanning will try to finish gracefully
# Default: 5000 (5 seconds)
GIT_SCAN_TIMEOUT_MS=5000

# Hard timeout for git repository scanning (milliseconds)
# This is the absolute maximum time before forcibly stopping the scan
# Default: 10000 (10 seconds)
GIT_SCAN_HARD_TIMEOUT_MS=10000

# Maximum depth to scan for git repositories
# Lower values = faster but may miss nested repos
# Higher values = slower but more thorough
# Default: 3 (omit or leave empty for unlimited)
GIT_SCAN_MAX_DEPTH=3

# ============================================================================
# Server Configuration (Optional)
# ============================================================================

# Backend server port (0 = auto-assign)
# BACKEND_PORT=0

# Frontend development server port
# FRONTEND_PORT=3000

# Backend server host
# HOST=127.0.0.1

# ============================================================================
# Development Settings
# ============================================================================

# Disable automatic git worktree cleanup (useful for debugging)
# DISABLE_WORKTREE_ORPHAN_CLEANUP=1

# Rust log level (trace, debug, info, warn, error)
# RUST_LOG=debug
